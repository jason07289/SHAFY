import UserApi from '@/apis/PostApi'

// initial state
const state = {
    posts : {}
}

// getters
const getters = {
  
}

// actions
const actions = {
// 포스트 crud

}

// mutations
const mutations = {
// posts 업데이트 
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}